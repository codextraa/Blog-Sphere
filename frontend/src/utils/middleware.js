// import { NextResponse } from "next/server";

// export async function tokenMiddleware(req) {
//   const { cookies } = req;
//   const access_token = cookies.get('access_token');

//   if (!access_token) {
//     return NextResponse.redirect(new URL('/login', req.url));
//   }

//   try {
    
//   }
// }